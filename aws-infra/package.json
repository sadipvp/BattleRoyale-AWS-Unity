{
  "name": "tank-battle-royale-infra",
  "version": "0.1.0",
  "description": "AWS CDK infrastructure for Tank Battle Royale",
  "main": "bin/app.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "synth": "npx cdk synth",
    "deploy": "npx cdk deploy",
    "destroy": "npx cdk destroy"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.130.0",
    "constructs": "^10.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "aws-cdk": "^2.130.0",
    "ts-node": "^10.9.0"
  }
}
