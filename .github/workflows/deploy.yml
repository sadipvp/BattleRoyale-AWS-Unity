# This file is superseded by per-service workflows:
#   deploy-auth.yml        — triggered by changes to services/auth/** or services/shared/**
#   deploy-matchmaking.yml — triggered by changes to services/matchmaking/** or services/shared/**
#
# Each workflow builds only the affected service's Docker image, pushes to ECR,
# and forces a new ECS deployment for that service.
